# nfpm config file: https://github.com/goreleaser/nfpm

name: "ssh_add_with_pass"
arch: "amd64"
platform: "linux"
version: "v${VERSION}"
maintainer: "Brad Jones <brad@bjc.id.au>"
description: Wrapper around ssh-add that uses expect to unlock the provided key.
homepage: "https://github.com/brad-jones/ssh-add-with-pass"
license: "MIT"
files:
  ./dist/ssh_add_with_pass_linux_amd64: /usr/local/bin/ssh_add_with_pass
